const compareButtons = document.querySelectorAll('.compare-container');

compareButtons.forEach(button => {
    button.addEventListener('click', () => {
       
    });
});

function initCompareBox(){
    
}